# Central feed-generation dependencies (GitHub Actions only).
# Users do NOT need these — user-side scripts only need requirements.txt.
# Note: twscrape requires Python >= 3.10.
-r requirements.txt
# Temporary 16-hex X web chunk fix from upstream PR #328.
# Use the tested commit archive so installation does not depend on a Git clone.
twscrape @ https://github.com/vladkens/twscrape/archive/3f47af40bc2036fe96562e82edf52b92b7fc2574.tar.gz
youtube-transcript-api>=0.6
yt-dlp>=2024.1
